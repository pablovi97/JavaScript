import { ResaltaVidaDirective } from './resalta-vida.directive';

describe('ResaltaVidaDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltaVidaDirective();
    expect(directive).toBeTruthy();
  });
});
